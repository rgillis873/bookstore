html
    head
        title Look Inna Book BookStore
        include ./adminHeader.pug
    body
        if orderList
            h4 Orders from Publishers
            br
            table.table
                thead
                    tr
                        th(scope='col' style='width: 20%;') Publisher
                        th(scope='col' style='width: 10%;') Quantity
                        th(scope='col' style='width: 20%;') Book Name
                        th(scope='col' style='width: 20%;') ISBN
                        th(scope='col' style='width: 30%;') Date
                tbody
                    for order in  orderList
                        tr
                            td #{order.pub_name}
                            td #{order.quantity}
                            td #{order.name}
                            td #{order.isbn}
                            td #{order.wo_date}
        else
            h4 No Orders have been made to publishers yet.